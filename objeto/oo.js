/* CÓDIGO NÃO EXECUTÁVEL!!!

// Paradigma Procedural
//Antes eram funções que tinham dados
processamento (valor1,valor2,valor3) //função que manipula dados

// Paradigma Orientação a Objetos
objeto = { //Funciona como uma casca, com atributos e funções encapsuladas
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

objeto.processamento() //Hoje são os dados (objetos) que possuem funções

Princípios Importantes:
1 - Abstração -> Obter as informações pertinentes para o determinado sistema, variando da demanda do cliente. Criar
variáveis que são realmente necessárias
2 - Encapsulamento ->  Evita o alto nível de acoplamento, o que gera dependências. Separação bem definida da 
implementação da função (encapsulada) e da interface que comunica os objetos entre si.
3 - Herança -> Receber atributos e métodos de uma classe pai. No JavaScript só existe um pai para cada componente filho.
Composição representa uma relação "tem um". Exemplo: "Carro tem um motor".
Na Herança existe a relação "é um". Exemplo: "Gol é um carro".
4 - Polimorfismo -> Os objetos podem assumir diferentes formas, podendo ser interpretado como o objeto pai, por exemplo
*/